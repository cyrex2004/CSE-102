#include <stdio.h>
int main()
{
    int k, n, num = 0;
    printf("Enter a Number: ");
    scanf("%d", &n);

    while (n != 0)
    {
        k = n % 10;
        num = num*10+k;
        n = n / 10;
    }

    printf("Reverse: %d", num);
    return 0;
}
