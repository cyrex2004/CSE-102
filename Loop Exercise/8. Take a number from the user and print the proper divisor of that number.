#include <stdio.h>
int main()
{
    int i,n;
    printf("Enter a Number: ");
    scanf("%d",&n);

    printf("Divisors= ");
    for(i=1;i<=n;i++)
        {
            if(n%i==0)
            {
                printf("%d ,",i);
            }
        }

    return 0;
}
