#include <stdio.h>
int main()
{
    int k, n, sum = 0;
    printf("Enter a Number: ");
    scanf("%d", &n);

    while (n != 0)  
    {
        k = n % 10;  
        sum += k;    
        n = n / 10;  
    }

    printf("Sum: %d", sum);
    return 0;
}
